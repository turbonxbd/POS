# Only the front controller is reachable inside /api.
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{REQUEST_FILENAME} !index\.php$
  RewriteRule ^ index.php [L,QSA]
</IfModule>

<FilesMatch "^(?!index\.php$).*\.php$">
  Require all denied
</FilesMatch>
